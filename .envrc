layout python3 .venv

# Silence direnv load messages.
export DIRENV_LOG_FORMAT=""

# Used by the shell prompt hook in ~/.bashrc.
export VIRTUAL_ENV_PROMPT="(crypto-predictor-system) "

source .venv/bin/activate